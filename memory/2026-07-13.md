# 2026-07-13 日誌

## ESP32 Portfolio App Parser Bug 修復（早上）

**完整流程（流程 A：驗證 → 改 → review → 編譯 → 燒錄 → 驗證）**

### 觸發原因
使用者請我做靜態檢查，發現 4 個 P0 parser bug：
1. `us_`（美股）完全沒被解析 → Portfolio 詳情頁永遠 $0
2. `cash_.usd_rate` / `cash_.jpy_rate` 沒解析 → Cash 詳情頁永遠 `@0.000`
3. `fx_.updated` 沒解析 → 匯率詳情頁 footer 永遠空白
4. `fx_.usd_twd` / `fx_.jpy_twd` 錨點 bug（沒撞上是因為 cash.rate_usd_twd 數值剛好相同）

順帶清理 P1：
5. `CreateHomeScreen()` 末段 10 行重複
6. 死 callback `HomeCardClickCallback`（cc + h）
7. 誤植 log（ParseWeather / ParsePortfolio 命名錯誤）

### Commit
`874214b7 [2026-07-13_0810] Fix portfolio parser: add us_/cash.rate_*/fx.updated + remove dead code`
- cc: 1426 → 1442 行（+16 淨增）
- h: 移除 1 行 callback 宣告
- Binary: `xiaozhi.bin` 4.02 MB（5MB partition 剩 18%）

### 燒錄
- 時間：2026-07-13 08:33
- 過程：30.9 秒（@ 738.8 kbit/s）
- USB：原本容器看不到 `/dev/ttyACM0`，使用者接上後才看到

### Serial log 驗證（燒完直接抓）
```
[TW]  mktval=4337703 gain=2517558 (138.32%)
[US]  mktval=2017439 gain=786654 (63.91%)             ← ✅ 之前從未出現
[FX]  usd_twd=32.0860 jpy_twd=0.1960 updated=[2026-07-13 08:30:38]   ← ✅ updated 有值
[Cash] USD=14228 in_twd=456526 rate=32.086, JPY=611791 in_twd=119911 rate=0.196   ← ✅ rate 有值
```

✅ 全部正常，無 panic、無 watchdog

### 教訓 / 待注意
- **一定要先驗 JSON 結構再開改**：這次用了「流程 A」（curl R2 → python -m json.tool）才避免改錯 key
- **靜態檢查 SOP** 是好習慣，使用者要求 review 才改 patch 內容是有道理的
- 之前我誤以為 `"updated"` parser 太脆弱，**用 web_fetch 看到的精簡格式是 artifact**（實際 R2 JSON 是 pretty 格式），後續類似檢查要用 `curl` 直接看原文
- 燒錄後的核心數字（總市值 6355142）跟 R2（6355896）會因資產波動略有不同，**這不是 bug**

### 還原點
```bash
cd /home/jhe/.openclaw/workspace/esp32-rlcd-project/02_Example/ESP32-S3-Touch-LCD-4.3B
git log --oneline -3   # 0ef9ed73 → bcc53efc → 874214b7 (HEAD)
git reset --hard 0ef9ed73   # 退回天氣功能完成版
```
