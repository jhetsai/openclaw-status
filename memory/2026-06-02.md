# 2026-06-02 收工狀態

## 當前配置
- **analyze_market_trend.py** 14:05 cron：✅ 正常
  - Model: `MiniMax-M2.7`（已改回，原本是 M3 但 timeout）
  - 優先吃 `taiwan_stock/commentary_YYYYMMDD.md`（A 路徑）
  - Fallback: M2.7 + 原本 6/1 短 prompt（B 路徑）
  - `dedup_repeats()` 修好 None 處理
- **commentary 檔案**：`/home/jhe/.openclaw/workspace/taiwan_stock/commentary_20260602.md`（v4，26K 字，8 段完整 + 國際總經 + 三大法人）

## 今日 PDF
- `taiwan_stock/market_report_20260602.pdf` (540KB)
- 17:48 v4 完整版，22:32 重發
- TG 已送出 ✅

## M3 API 問題（重要）
- `https://api.minimax.io/anthropic/v1/messages` endpoint
- 對 **長 prompt + 長回應**（>8K tokens）會 180s+ timeout
- M2.7 走同一 endpoint 但**用原本短 prompt 沒事**（30-60s 回應）
- M3 + 短 prompt 10.5s OK，但**長內容就卡**
- 結論：先別碰 M3 API，腳本用 M2.7 fallback

## 重要發現
- M2.7 vs M3 比較：M2.7 預設「詳盡」，M3 預設「精簡」
- M3 雖然小，但 prompt 設計好可以達到 M2.7 等級（v3/v4 證明）
- 失敗原因不是模型，是 API 連線 buffer 問題
- 6/1 成功的 PDF 是 M2.7 + 短 prompt

## 流程 SOP（建立 A+B 機制）
1. 14:05 cron 跑 `analyze_market_trend.py`
2. 先找 `taiwan_stock/commentary_YYYYMMDD.md`：
   - 有 → 吃檔案（A 路徑，M3 等級品質）
   - 沒有 → 跑 M2.7 自動生成（B 路徑）
3. 產 PDF → 上傳 R2 → 發 TG

## 下次可改進
- 研究 M3 API streaming 或其他 endpoint
- OpenRouter 上的 M3 是否更穩定
- 寫 commentary 給我時，提供完整 PG 數據（漲幅 Top 5、成交量 Top 15、跌幅 Top 5）
