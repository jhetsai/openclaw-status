## 發電記錄
- 09:40 發電量：42W（陰天，局部多雲）
## 發電記錄
- 09:40 發電量：42W（陰天，局部多雲）

## 配息系統更新（2026-05-02）

### 已完成
- **gen-stock-html.py** 整合 Yahoo 配息動態抓取（HTMLParser 解析）
- 已入帳：53,220 元，待發放：26,466 元（驗證符合對帳單 53,221）
- HTML 明細改用 `<details>` 摺疊顯示
- R2 上傳目前在這台機器上有 DNS 解析問題（`shared-files.chewei.com.tw`）

### 規則（已驗證）
- Yahoo 配息表格：`<li>` 內的 `<div>` 結構，period 在 `<div>xxxxQ1</div>` 標籤
- 現金股利：在 `<span>` 標籤內，緊接在 period 文字後
- 日期：直接出現，無 `<span>` 包裝，順序是「除息日、發放日」
- 支援格式：`2026Q1`（季配息）和 `2026M4`（月配息）
- 計算：`股數 × 每股股利`（不是 ÷10）
- 以「**入帳日（payout）**」判斷已入帳或待發放
