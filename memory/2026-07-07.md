# 2026-07-07 每日紀錄

## Board 型號（重要）
**ESP32-S3-Touch-LCD-4B (Waveshare)**
- ESP32-S3 X1 (240MHz) + 4.3" IPS Touch (480x272)
- 8MB PSRAM, 16MB Flash
- USB-C 供電 + 燒錄
- CH340 USB-to-Serial

## 發電記錄（累積）
- 2026-07-07：早晨 29°C/84%濕度/6km/h，Partly cloudy，UV 1

## ESP32 PortfolioDashboard 韌體開發（2026-07-07）

### 硬體連接
- 燒錄埠：`/dev/ttyACM0`
- 燒錄速度：460800 baud
- 燒錄工具：esptool.py (arduino15 packages)

### 今日進度
1. **09:xx**：誤將 XiaoZhiCode_V2.1.0 燒成 Weather Clock（Arduino/11_Weather_Clock），LCD 顯示混亂
2. **10:xx**：刷回 XiaoZhiCode_V2.1.0（07-06 16:55 build）
3. 確認 WiFi SSID 掃描正常（CHT Wi-Fi、IoT）
4. 確認 IoT 路由器密碼 `057851463`
5. 確認韌體燒入位置正確（`idf.py build` + `esptool write_flash`）

### 修復問題
1. **WiFi Scan 列表空白**：`lvgl_port_lock(10ms → 100ms)` ✅
2. **UpdateUi() watchdog timeout**：所有 `UpdateUi()` 改 `update_pending_` flag ✅
3. **WiFi polling 與 XiaoZhi 衝突**：改用 `SetNetworkEventCallback` ✅
4. **wifi_status_big_label_ null**：加 null check + 必要時重建 ✅

### 今日重大發現
- `SetNetworkEventCallback` 從未被呼叫（根本原因）
- XiaoZhi WifiStation 自動管理 WiFi，PortfolioApp 應透過 callback 接收 Connected 事件
- 最新修復已 commit：`215e567`

### 待修復（P0）
1. **燒錄中斷**：esptool 被 SIGKILL 中斷，最後一版韌體未燒入
2. **WiFi Connected callback**：需燒入新韌體後驗證 `SetNetworkEventCallback` 是否正常 trigger fetch
3. **首頁大字 WiFi 狀態**：需確認 LCD 是否顯示 `✅ WiFi OK`

## WiFi 路由器
- SSID: IoT
- 密碼: 057851463
- 狀態：已確認密碼正確，ESP32 可成功連線（IP: 192.168.68.61）

## Git Commit
- `215e567` Add PortfolioDashboard with WiFi event callback integration
- `4c15ccd` Add PortfolioDashboard application (ESP32-S3 LCD 4B)

## 待觀察
1. 燒入新韌體（SetNetworkEventCallback 版本）後，WiFi 連線是否自動 fetch 天氣/持股
2. 首頁是否顯示 `✅ WiFi OK`（綠色大字）
3. 天氣是否從 `--°C` 變成 actual 度數
4. 持股是否從 `$0` 變成 actual 金額
